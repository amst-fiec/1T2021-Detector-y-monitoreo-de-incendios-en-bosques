<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grupo5AMST.livingforest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo5"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LivingForest"
        android:versionCode="2"
        android:versionName="2.0">
        <activity android:name=".vistualiazrDatosTotales"></activity>
        <activity android:name=".SinConexion" />
        <activity android:name=".RegistroDispositivos" />
        <activity android:name=".DispositivosRegistrados" />
        <activity android:name=".MenuDispositivo" />
        <activity android:name=".Menu" />
        <activity android:name=".InicioSesion" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>